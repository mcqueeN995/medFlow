# Секреты и локальные оверрайды — реальные значения только на машине/сервере,
# в репозиторий попадают лишь .env.example и .env.production (шаблоны-плейсхолдеры).
.env
.env.*
!.env.example
!.env.production
*.save

.DS_Store
