# @pbe/react-yandex-maps не обновлял peer-deps под React 19 (сам wrapper
# не завязан на внутренности React, конфликт чисто версийный) — без этого
# npm ci падает с ERESOLVE и в докере, и локально.
legacy-peer-deps=true
